# an endpoint needs at least 
# default for request.path is "/" and request.methode is "GET"
# default for response code is 204
endpoints:
  - request:
      path: "/wildcard/*/foo"
    response:
      statusCode: 204