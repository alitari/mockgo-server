endpoints:
  - id: "maximal"
    request:
      method: "POST"
      path: "/maximal"
      query:
        firstQueryParam: value1
        secondQueryParam: value2
      headers:
        Content-Type: "application/json"
        Myheader: myheaderValue
      body: |
        {
          "mybody": "is max"
        }
    response:
      statusCode: 204